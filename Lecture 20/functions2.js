function calculateShoppingCartPrice(...price1) // Rest parameter // Note -> It looks the same as spread operator but the use case is different
{
    return price1 
}
console.log(calculateShoppingCartPrice(100, 200, 500)) // [ 100, 200, 500 ]
