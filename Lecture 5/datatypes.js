"use strict"; // Treats all JS code as newer version

let name = "Saransh" // String
let age = 18 // Integer
let isLoggedIn = true // Boolean
let gender = 'M' // Character

console.table([name, age, isLoggedIn, gender]);

// Undefined = Variable initialized but no value associated to it
// NULL = It is a representation of an empty value
// Symbol = Used to identify uniqueness

console.log(typeof age) // "typeof" is used to find the type of a variable
// console.log(typeof NULL) = object
// console.log(typeof undefined) = undefined